# Grant GPU access to all processes started by Zygote.
# They need that to render the standard UI. steal from headhammer
allow appdomain powervr_device:chr_file rw_file_perms;
allow appdomain mali_device:chr_file rw_file_perms;